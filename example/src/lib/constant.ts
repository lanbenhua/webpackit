export const ProductCode = 'studio';

export const Routers = {
  HOME: `/${ProductCode}/home`,
  TEST: `/${ProductCode}/test`,
};

// pls don't change the order
export enum EProjectRole {
  admin = 'admin',
  ops = 'ops',
  dev = 'dev',
}
